# EduAgent Connect — Client environment example
# Copy to .env.local and adjust as needed. Do NOT commit .env.local.

# Base URL of the backend REST API. Exposed to the browser, so must be public.
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
